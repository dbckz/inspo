[project]
name = "inspo"
version = "1.0.0"
description = "Inspirational quotes fullscreen display app"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
inspo = "inspo_app:main"

[tool.uv]
dev-dependencies = []
